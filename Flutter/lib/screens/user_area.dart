import 'package:flutter/material.dart';

class UserAreaScreen extends StatelessWidget {
  UserAreaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Hola"),
        ),
      ),
    );
  }
}
